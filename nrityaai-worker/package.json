{
	"name": "nrityaai-worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "npm run setup-db && wrangler deploy",
		"setup-db": "wrangler d1 execute nrityaai-db --file=schema.sql --remote && node seed-db.js > seed.sql && wrangler d1 execute nrityaai-db --file=seed.sql --remote",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"@cloudflare/workers-types": "^4.20251126.0",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.51.0"
	}
}
